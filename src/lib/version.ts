// Bump this every time you cut a new release, matching the git tag you
// push (without the "v" -- e.g. tag `v1.1` pairs with APP_VERSION '1.1').
export const APP_VERSION = '1.0'

// Fill this in once you've pushed this project to GitHub, as
// "your-username/your-repo-name". The update checker uses it to look up
// the latest public release. Leave it blank to disable update checks
// (e.g. while still testing locally before the repo exists).
export const GITHUB_REPO = ''
